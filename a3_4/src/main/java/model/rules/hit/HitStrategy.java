package model.rules.hit;

import model.rules.Visitable;

/**
 * For creating a hit strategy that will accept a Visitor object.
 */
public abstract class HitStrategy implements IntfHitStrategy, Visitable {}