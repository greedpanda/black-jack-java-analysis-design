package model.rules.win;

import model.rules.Visitable;

/**
 * For creating a win strategy that will accept a Visitor object.
 */
public abstract class WinStrategy implements IntfWinStrategy, Visitable {}
